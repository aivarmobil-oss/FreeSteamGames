<ui:FluentWindow x:Class="FreeSteamGames.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FreeSteamGames"
        xmlns:models="clr-namespace:FreeSteamGames.Models"
        xmlns:converters="clr-namespace:FreeSteamGames.Converters"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Free Games Radar" Height="640" Width="920"
        Icon="/Assets/tray.ico"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Closing="FluentWindow_Closing">

    <Window.Resources>
        <converters:StringToVisibilityConverter x:Key="NullToVisibilityConverter" />

        <Style x:Key="GameCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="ClipToBounds" Value="True" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="0" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                To="{StaticResource ColorSurfaceHover}" Duration="0:0:0.15" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                                 To="-3" Duration="0:0:0.15" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                To="{StaticResource ColorSurface}" Duration="0:0:0.2" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y"
                                                 To="0" Duration="0:0:0.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="GameCardTemplate" DataType="{x:Type models:FreeGame}">
            <Border Style="{StaticResource GameCardStyle}" Width="300" Margin="0,0,16,16">
                <StackPanel>
                    <Border Height="140" Background="Black" ClipToBounds="True"
                            CornerRadius="8,8,0,0">
                        <Image Source="{Binding HeaderImageUrl}" Stretch="UniformToFill" />
                    </Border>

                    <Rectangle Height="3" Fill="{StaticResource AccentBrush}" />

                    <StackPanel Margin="14,12,14,14">
                        <TextBlock Text="{Binding Name}" FontFamily="{StaticResource DisplayFont}"
                                   FontSize="16" Foreground="{StaticResource TextPrimaryBrush}"
                                   TextWrapping="Wrap" Height="44" TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Top" />

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="{Binding FormattedOriginalPrice}"
                                       FontFamily="{StaticResource NumericFont}"
                                       TextDecorations="Strikethrough"
                                       Foreground="{StaticResource TextMutedBrush}" Margin="0,0,10,0" />
                            <Border Background="{StaticResource AccentDimBrush}" CornerRadius="4" Padding="7,2">
                                <TextBlock Text="{Binding DataContext.Loc[free_badge], RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                           Foreground="{StaticResource AccentBrush}" FontSize="11"
                                           FontFamily="{StaticResource DisplayFont}" />
                            </Border>
                        </StackPanel>

                        <TextBlock Text="{Binding TimeLeftLabel}" FontFamily="{StaticResource NumericFont}"
                                   Foreground="{StaticResource TextMutedBrush}" FontSize="12"
                                   Height="18" Margin="0,4,0,0" />

                        <ui:Button Content="{Binding DataContext.Loc[grab_button], RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                   Appearance="Primary" HorizontalAlignment="Stretch" Margin="0,12,0,0"
                                   Command="{Binding DataContext.OpenStorePageCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                   CommandParameter="{Binding}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Free Games Radar" FontFamily="{StaticResource DisplayFont}" />

        <Border x:Name="UpdateBanner" Grid.Row="1" Margin="16,10,16,0" Padding="16,12"
                Background="{StaticResource WarningSurfaceBrush}"
                BorderBrush="{StaticResource WarningBorderBrush}" BorderThickness="1" CornerRadius="8"
                Opacity="0" RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TranslateTransform Y="-8" />
            </Border.RenderTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding UpdateBannerVisible}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" To="0" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15" />
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.15">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Loc[update_banner_title]}" FontFamily="{StaticResource DisplayFont}"
                               Foreground="{StaticResource WarningBrush}" />
                    <TextBlock Text="{Binding UpdateBannerMessage}" Foreground="{StaticResource WarningBrush}"
                               Opacity="0.85" TextWrapping="Wrap" Margin="0,3,0,0" />
                </StackPanel>

                <ui:Button Grid.Column="1" Content="{Binding Loc[update_banner_download]}"
                           Command="{Binding OpenReleasesPageCommand}"
                           Appearance="Primary" VerticalAlignment="Center" Margin="12,0,0,0" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding FreeGames}"
                              ItemTemplate="{StaticResource GameCardTemplate}"
                              Margin="16">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1" Duration="0:0:0.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Text="{Binding StatusMessage}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{StaticResource TextMutedBrush}" FontSize="14"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}" />

            <ui:ProgressRing IsIndeterminate="{Binding IsLoading}"
                              Foreground="{StaticResource AccentBrush}"
                              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</ui:FluentWindow>
